apiVersion: v1
kind: Service
metadata:
  name: cst407net-service
  namespace: cst407-deployment
spec:
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: cst407
